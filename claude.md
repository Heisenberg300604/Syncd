
# SyncD

You are working on the SyncD codebase.

Read these files before implementing significant changes:

- `AGENTS.md` — engineering rules
- `docs/ARCHITECTURE.md` — actual architecture
- `docs/PRODUCT.md` — product requirements
- `docs/ROADMAP.md` — development phases

Important:

- Inspect the existing code before modifying it.
- Reuse the existing architecture.
- Do not rewrite working features.
- Keep changes scoped to the requested task.
- Backend is authoritative for authentication and authorization.
- Use strong TypeScript practices.
- Verify changes before declaring them complete.

The existing codebase is the source of truth.